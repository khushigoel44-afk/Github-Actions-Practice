name: pr-check

on:
    pull_request:
        branches: [main]
        types: [opened, synchronize]

jobs:
    pr:
        runs-on: ubuntu-latest
        steps:
            - name: PR check running message
              run: echo "PR check running for branch: ${{github.ref_name}}"
    
