name: artifact-workflow

on: 
    push:

jobs:
    artifact:
        runs-on: ubuntu-latest
        steps:
            - name: Write the data into the file
              run: |
                  echo "Hey there!" > test_report.log
                  echo "Khushi this side" >> test_report.log
                  echo "How are you" >> test_report.log

            - name: Upload the artifacts
              uses: actions/upload-artifact@v4
              with:
                  name: test_report
                  path: test_report.log
                
